# Adhaar AI — Python dependencies
# Production-pinned versions for Render deployment

# Web server
flask==3.0.3
gunicorn==22.0.0          # Production WSGI server (replaces dev server)

# Data & ML
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
joblib==1.4.2

# Google Gemini (optional — for AI chat)
google-generativeai==0.7.2

# HTTP utils (used by gunicorn health checks)
requests==2.32.3
